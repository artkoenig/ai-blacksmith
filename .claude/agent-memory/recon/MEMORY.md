# recon — Memory

Index. Eine Zeile pro Eintrag, Details in den Themendateien daneben.

## Layout
- Repo enthält bisher nur Konfiguration (`CLAUDE.md`, `.claude/`, `docs/`) und `LICENSE`.

## Kommandos
- build/test/lint: noch nicht festgelegt → siehe `commands.md`, sobald der Stack steht.

## Fallstricke
- (noch keine)
